# SAM2.1 Tiny 三锚点小规模测试

## 结论

三 PF 锚点不能替代当前的人工重锚流程，也不应直接接入正式链路做自动轨道选择。它适合作为冲突诊断信号：只有至少两条 SAM 轨道达到高一致时才标记 `supported`，否则保持 `no_consensus` 并进入人工复查。

## 测试设计

- 模型：远端 `sam2.1_hiera_tiny` 服务。
- 锚点：每个 clip 的 25%、50%、75% 附近最近有效 PF bbox。
- 测试集：3 个已人工重锚案例 + 2 个原 75% 锚点已确认案例。
- 参照：GUI 人工最终选择的完整 SAM 轨道，不作为独立 GT 准确率。
- 共识：至少一对 SAM 轨道的逐帧 IoU 中位数不低于 0.7；无合格轨道对时禁止强行选择。

## 结果

| Clip | 锚点帧 | 各锚点相对最终轨道 IoU mean | 共识 |
| --- | --- | --- | --- |
| `9905284100029/clip_002` | 11 / 21 / 31 | 0.067 / 0.000 / 0.025 | no_consensus |
| `9905284100089/clip_003` | 10 / 19 / 28 | 0.067 / 0.006 / 0.024 | no_consensus |
| `9905284100098/clip_003` | 5 / 10 / 15 | 0.181 / 0.050 / 0.242 | no_consensus |
| `9905284100031/clip_001` | 9 / 18 / 27 | 0.001 / 0.054 / 1.000 | no_consensus；只有 75% 锚点正确 |
| `9905284100040/clip_002` | 5 / 10 / 15 | 0.028 / 0.746 / 1.000 | supported；50%/75% 一致，保留 75% 主锚点 |

5 个 clip 中仅 1 个形成可靠双轨共识，并且共识保留的 75% 锚点与人工最终选择一致。3 个错误 PF 案例中，三个 PF 锚点全部无法接近人工重锚轨道，说明多取几个 PF 锚点并没有打破 PF 错误来源。

## 性能

- 15 次 SAM2 双向传播。
- 共传播 465 帧。
- 服务端推理总耗时 59.10 秒。
- 汇总吞吐约 7.87 FPS。
- 顺序执行不增加峰值显存，但相对单锚点约增加 3 倍推理时间。

## 接入决定

新增 `tools/benchmark_sam2_multi_anchor.py`，已经通过当前 SAM2 REST API 完成三锚点传播、原始结果保存、两两一致性计算、人工最终轨道对照和可视化视频生成。

正式 PF/SAM QC 主流程继续保留 75% 单锚点。多锚点暂不自动替换最终轨道；后续只能作为冲突后的可选诊断层，`no_consensus` 必须进入 GUI，不能选择分数最高的一条轨道。

完整机器结果位于测试产物目录的 `multi_anchor_small_test/summary.json`，每个 clip 下包含 `report.json`、三份 raw JSON 和 `multi_anchor_comparison.mp4`。
